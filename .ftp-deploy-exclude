# Reference list of what stays OFF the Hostinger server.
# The authoritative copy is the `exclude:` block in .github/workflows/deploy.yml —
# keep the two in sync. The prebuilt/prerendered dist/ IS shipped now (Hostinger
# serves it as-is and must not rebuild), so dist/ is intentionally NOT excluded.

.git/**
.github/**
.cursor/**
.claude/**
node_modules/**
src/**
public/**
e2e/**
scripts/**
test-results/**
playwright-report/**
blob-report/**
playwright/.cache/**
deploy-artifacts/**
**/.DS_Store
.env
.env.*
!.env.example
*.local
*.log
index.html
vite.config.ts
tsconfig*.json
playwright.config.ts
.oxlintrc.json
*.md
